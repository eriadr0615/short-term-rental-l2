@{
    int pagina = ViewBag.Pagina ?? 1;
    int totalPaginas = ViewBag.TotalPaginas ?? 0;
    string? buscar = ViewBag.Buscar as string;
    int? idReserva = ViewBag.IdReserva as int?;
    bool? disponible = ViewBag.Disponible as bool?;
    string? dni = ViewBag.Dni as string;
    int? dias = ViewBag.Dias as int?;
    string? fechaInicio = ViewBag.FechaInicio as string;
    string? fechaFin = ViewBag.FechaFin as string;
}

@if (totalPaginas > 1)
{
    <nav aria-label="Paginación">
        <ul class="pagination">
            <li class="page-item @(pagina <= 1 ? "disabled" : "")">
                <a class="page-link"
                   asp-route-pagina="@(pagina - 1)"
                   asp-route-buscar="@buscar"
                   asp-route-idReserva="@idReserva"
                   asp-route-disponible="@disponible"
                   asp-route-dni="@dni" asp-route-dias="@dias"
                   asp-route-fechaInicio="@fechaInicio" asp-route-fechaFin="@fechaFin">Anterior</a>
            </li>

            @for (int numero = 1; numero <= totalPaginas; numero++)
            {
                <li class="page-item @(numero == pagina ? "active" : "")">
                    <a class="page-link"
                       asp-route-pagina="@numero"
                       asp-route-buscar="@buscar"
                       asp-route-idReserva="@idReserva"
                       asp-route-disponible="@disponible"
                       asp-route-dni="@dni" asp-route-dias="@dias"
                       asp-route-fechaInicio="@fechaInicio" asp-route-fechaFin="@fechaFin">@numero</a>
                </li>
            }

            <li class="page-item @(pagina >= totalPaginas ? "disabled" : "")">
                <a class="page-link"
                   asp-route-pagina="@(pagina + 1)"
                   asp-route-buscar="@buscar"
                   asp-route-idReserva="@idReserva"
                   asp-route-disponible="@disponible"
                   asp-route-dni="@dni" asp-route-dias="@dias"
                   asp-route-fechaInicio="@fechaInicio" asp-route-fechaFin="@fechaFin">Siguiente</a>
            </li>
        </ul>
    </nav>
}
