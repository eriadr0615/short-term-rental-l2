@{
    ViewData["Title"] = "Acceso restringido";
}

<div class="alert alert-warning">
    <h1 class="h4">Acceso restringido</h1>
    <p class="mb-0">Su usuario no tiene permiso para realizar esta operación.</p>
</div>

<a asp-controller="Home" asp-action="Index" class="btn btn-secondary">Volver al inicio</a>
